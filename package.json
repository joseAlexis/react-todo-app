{
  "name": "react-todo-monorepo",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "dev:api": "pnpm --filter api dev",
    "dev:all": "concurrently \"pnpm dev\" \"pnpm dev:api\"",
    "build": "pnpm --filter web build",
    "start": "pnpm --filter web preview",
    "lint": "pnpm --filter web lint",
    "lint:fix": "pnpm --filter web lint:fix",
    "format": "pnpm --filter web format",
    "format:check": "pnpm --filter web format:check",
    "prepare": "husky install",
    "lint-staged": "pnpm --filter web lint-staged"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "husky": "^8.0.0"
  }
}
